# 掌厨 — 开发日志

## 2026年8月16日（周日）— 语义搜索得分阈值 + 上线真机实测

### 🔧 优化
- `agent.js`：`preSearch` 语义检索加得分阈值 `SEM_MIN_SCORE = 0.35`——最高分低于阈值判定「没真匹配上」，降级关键词兜底，避免「问天气也硬推菜谱」的低分蒙对

### 🚀 上线
- commit `8e52de3`，push 到 GitHub Pages（8/15 语义搜索成果 + 8/16 得分阈值一起上线）

### 📊 真机实测（三测全绿）
| 查询 | 结果 |
|------|------|
| 今天天气怎么样 | ✅ 不乱推菜，诚实承认不知道天气 |
| 早上吃什么 | ✅ 5 道早餐菜，全部有跳转按钮 |
| 健身吃什么 | ✅ 减脂+高蛋白双方向（同义词映射 + AI 语义扩展）|

### 💡 经验
- 语义检索的「命中」可能是蒙对（得分低），要看得分设阈值，不能只看 top-1 命中块
- 阈值 0.35 卡得刚好：拦得住「天气」这类无关问题，放得进「早餐/健身」正常问题

---

## 2026年8月15日（周五）— AI 助手接入语义搜索（bge）

### 🧠 功能
- AI 助手「搜菜谱」从**纯子串匹配**升级为**语义向量检索**：本地跑 `bge-small-zh-v1.5`（q8 量化），真懂「早上吃什么→早餐」「鸡蛋能做什么→所有蛋菜」
- 懒加载：打开 AI 助手才后台下载模型（~24MB），未就绪前走关键词兜底，不阻塞首屏

### 📦 新增文件
- `semantic-search.js` — 浏览器端语义搜索模块（动态 import transformers.js，`env.localModelPath` 指向自托管模型，`dtype=q8`）
- `models/Xenova/bge-small-zh-v1.5/` — 自托管 bge 模型（config/tokenizer + `model_quantized.onnx` 23MB），同源加载避开 hf-mirror 的 CORS
- `data/recipe-vectors.json` — 159 道菜谱预计算语义向量（590KB，512 维四舍五入 4 位）

### 🔧 改动文件
- `agent.js`：`executeSearchRecipes` 语义优先/关键词兜底；`executeCookingTool`、`preSearch` 改 async；`preSearch` 用原始消息做语义检索（非食材列表时）
- `index.html`：引入 `semantic-search.js`；`toggleChat()` 打开时触发 `__semantic.init()`
- `sw.js`：CACHE_VERSION v5→v6；`/models/` 大文件走无超时网络优先（否则 3 秒超时会打断 23MB 下载）；预缓存 semantic-search.js / agent.js / recipe-vectors.json

### ⚠️ 关键决策与坑
- **必须自托管模型**：hf-mirror 的 CORS 只放行自身域名，浏览器运行时直连会被拦；提交进仓库同源加载才无 CORS
- **量化是必须的**：fp32 模型 91MB，q8 量化 23MB，才能接受下载
- **SW 3 秒超时陷阱**：原 SW 本地文件 3 秒超时回退，23MB 下载必被打断 → 单独给 `/models/` 开无超时通道

### 📊 验证
- `node --check` 三个 JS 文件语法通过
- Node 端到端：用自托管 q8 模型 + 压缩向量跑真实查询，「早上吃什么」「鸡蛋能做什么」全命中
- 待上线：push 到 GitHub Pages 后真机/浏览器实测模型下载 + WASM 推理

### 🐛 后续修复（同日）

**问题 1：推荐多道菜只有一张可点卡片。** AI 回复列了 4 道菜，但原代码只提取第一道菜名渲染一张卡片，点击只能去那一道。

- 根因：从 AI 文字回复里「抠菜名」做文本匹配，AI 常缩写菜名（「蒸水蛋」vs「蒸水蛋（鸡蛋羹）」，「蒜蓉西兰花」vs「蒜蓉西兰花炒虾仁」），必然漏配
- 修法：`cookingAgent` 返回值改为 `{ reply, recipeIds }`，把语义/标签/agent 工具检索到的**真实菜谱 id** 一路带出来；`getAiRecommendation` 按 id 渲染多张卡片（点哪道进哪道）
- `agent.js`：新增 `collectRecommendationIds`；`agentLoop` 收集工具返回里的菜谱 id
- `index.html`：新增 `extractRecommendedIds`（核心名匹配兜底）+ `goToRecipe(id)`

**问题 2：语义向量对口语词匹配弱。** 搜「想吃健身吃的」，bge 返回「蛋炒方便面/鸡蛋灌饼」（「健身」对不上「减脂」标签）。

- 修法：新增 `TAG_INTENTS` 同义词映射表（18 标签全覆盖）+ `detectTagIntent()`，`preSearch` 优先级改为：食材列表 → **标签意图** → 语义 → 关键词
- 只放「意图词」（健身/减肥/早饭/快手/请客/凉拌/麻辣…），不放单字食材词（汤/虾/鱼），避免具体菜名误命中
- 22 条测试全过：口语词精准命中标签，具体菜名（番茄炒蛋/皮蛋瘦肉粥）不误判

---

## 2026年8月2日（周日）— 线上白屏紧急修复

### 🐛 问题现象
- 线上版 https://burgerman777.github.io/cooking-app 打开完全空白，无任何内容显示
- 本地启动正常，定位为部署版本 bug

### 🔍 根因分析
**两个致命问题叠加：**

1. **`recipes.js` JS 语法错误**：`chaofangbianmian`（蛋炒方便面）菜谱对象末尾少了一个 `},`，导致整个 500KB 的 JS 文件解析失败 → `DEFAULT_RECIPES` 变成 `undefined` → 所有 JS 崩溃 → 页面白屏

2. **`recipes.js` 放在 `<head>` 同步加载**：500KB 的菜谱数据阻塞页面渲染，拖慢首屏

### 🔧 修复内容（commit `f838778`）
- `recipes.js`：补上蛋炒方便面菜谱后缺失的 `},`
- `index.html`：
  - `recipes.js` 从 `<head>` 移到底部 `</body>` 前，不再阻塞渲染
  - CDN 脚本（lz-string、qrcode、html5-qrcode）加 `defer` 属性
  - 新增骨架屏（skeleton card），首屏不再空白等待
  - `loadRecipes()` 增加 `DEFAULT_RECIPES` 防御检查，JS 加载失败时优雅降级
  - 增加 DNS 预解析（dns-prefetch）优化 CDN 连接
- `sw.js`：CACHE_VERSION v4 → v5，CDN 资源改为 stale-while-revalidate 策略，网络超时/失败优雅降级

### 💡 经验教训
- **PWA 缓存陷阱**：即使代码修好 push 了，旧 Service Worker 仍会缓存旧文件。用户需 Ctrl+Shift+R 强制刷新或通过页面「🔄 强制刷新」按钮才能看到新版本
- **JS 语法错误定位**：500KB 的单文件菜谱数据库，少一个 `},` 整个 App 就崩了。未来可考虑菜谱分文件加载或用 JSON 格式（天然防语法错误）
- **防御性编程**：`typeof DEFAULT_RECIPES === 'undefined'` 检查是最后防线，即使外部 JS 加载失败也能给用户一个空状态而非白屏

### 📊 线上验证
- 服务器返回正确（HTTP 200），HTML 结构正常
- `recipes.js` 语法检查通过（node --check）
- SW v5 部署确认，CDN stale-while-revalidate 生效
- 用户验证：线上恢复正常 ✅

---

## 2026年7月20日（周日）— 部署上线 + 移动端适配

### 🚀 GitHub Pages 上线
- 仓库：`github.com/burgerman777/cooking-app`
- 网址：`https://burgerman777.github.io/cooking-app`
- Git 初始化、配置、首次推送

### 🔒 发布前优化
- 设置面板新增**隐私说明**（数据仅存本地、不上传服务器）
- API Key 区域新增 DeepSeek 获取链接引导
- **CDN 降级**：lz-string/qrcode/html5-qrcode 加载失败时友好提示替代方案
- SW 缓存版本管理（`CACHE_VERSION`），每次部署强制刷新
- 顶部栏新增 **🔄 强制刷新按钮**（一键清缓存+注销SW+重载）

### 🐛 导航衔接修复
- **备菜→做菜悬浮窗闪现**：移除按钮处理器中多余的 `stopTimer()`
- **做菜页退回按钮无效**：`goPrep()` 从 `goScreen('prep')` → `history.back()` → 最终用 `goScreen('prep')` 直接跳转
- **做菜第一步底部按钮**：从灰色不可点的「上一步」改为可点的「← 返回备菜」
- 根因：`stopSpeak()` 里 `speechSynthesis.cancel()` 无保护，抛错后 `goScreen` 永远不执行 → 所有 try/catch 加固

### 🎵 语音系统 — Android 攻坚战
- **结论**：Android 国产浏览器（微信、厂商自带）**不支持** `speechSynthesis` API
- 音色面板从「加载音色列表」改为「实测播报」（▶ 测试播报按钮）
- `getVoiceList()`/`openVoiceSelector()`/`speakStep()` 全程 try/catch 保护
- 启动时自动检测语音是否可用，不可用时弹一次性提示
- 浏览器识别：微信/QQ/UC/厂商自带 → 分别给出不同引导
- **唯一解决方案**：用 Google Chrome 浏览器，或重写为微信小程序

### 📱 移动端 JS 陷阱总结
- `![]` 是 `false`（空数组检查必须用 `.length`）
- 模板字符串 `` `${}` `` 和展开运算符 `[...arr]` 旧手机不支持
- CSS 变量设 inline style（`btn.style.background = 'var(--)'`）移动端不解析
- `scrollIntoView` 在隐藏元素上可能抛错
- `addEventListener` 在动态启用的 `disabled` 按钮上不可靠 → 改用 `onclick`

### 🤖 下一步计划
- **微信小程序版**：用微信同声传译插件替代 `speechSynthesis`，解决语音兼容性
- 需重写：`.wxml` 页面结构、`.wxss` 样式、微信 API 数据存储
- 大部分 JS 逻辑可复用

---

## 2026年7月18日（周四）

### 🍳 菜谱扩充：4 → 51 道

全天从 4 道扩充到 51 道菜谱，每道菜经过 WebSearch 交叉验证用量和步骤：

| 批次 | 数量 | 代表菜品 |
|------|------|---------|
| 原有 | 4 | 番茄炒蛋、可乐鸡翅、京酱肉丝、酸辣土豆丝 |
| 第1批 入门快手 | 7 | 凉拌黄瓜、蒸水蛋、蒜蓉空心菜、手撕包菜、肉末豆腐、韭菜炒鸡蛋、虎皮青椒 |
| 第2批 蔬菜汤 | 7 | 白灼生菜、洋葱炒蛋、蒜蓉西蓝花、紫菜蛋花汤、香菇油菜、地三鲜、红烧茄子 |
| 第3批 经典下饭 | 7 | 红烧肉、宫保鸡丁、麻婆豆腐、清蒸鲈鱼、糖醋里脊、回锅肉、鱼香肉丝 |
| 第4批 荤菜快手 | 7 | 木须肉、青椒肉丝、红烧排骨、蒜蓉粉丝蒸虾、蚝油牛肉、干煸四季豆、葱油鸡 |
| 第5批 川味主食 | 8 | 水煮肉片、红烧带鱼、土豆炖牛肉、蛋炒饭、阳春面、饺子、番茄蛋花汤、冬瓜排骨汤 |
| 第6批 终极收尾 | 11 | 炸酱面、腊肠焖饭、馄饨、皮蛋瘦肉粥、酸辣汤、三杯鸡、水煮鱼、椒盐虾、酸辣粉、煎牛排 + 番茄炒蛋甜口版 |

每道菜包含：精确克数/ml数、每步秒数、视觉判断提示、"为什么这样做"的小贴士。

### 📁 架构重构：数据与 UI 分离

- **之前**：4200+ 行单文件，51 道菜谱数据嵌入 `index.html`
- **之后**：`recipes.js`（1621 行，纯菜谱数据）+ `index.html`（2740 行，纯 UI 逻辑）
- `index.html` 通过 `<script src="recipes.js"></script>` 加载数据
- 新增 `loadRecipes()` 自动合并逻辑：localStorage 旧数据缺 `tags` 等新字段时，自动从 DEFAULT_RECIPES 补齐

### ⚙️ 管理后台重构

- **设置面板**：备菜方式、计时器模式、API Key、音色管理整合为顶部折叠面板
- **菜谱管理**：在设置下方，新增搜索框（支持搜菜名/emoji/难度/标签，命中关键词高亮）
- **首页**：新增搜索框和标签筛选栏
- **数据迁移**：导出文件 / 导入文件 / 二维码分享 / 扫码导入

### 🏷️ 标签系统

51 道菜全部标注 13 种标签：快速、下饭、荤菜、海鲜、主食、汤羹、凉菜、早餐、宴客、减脂、川菜、粤菜、鲁菜

首页标签栏可筛选，支持与搜索叠加。标签栏和排序栏添加桌面端 ◀ ▶ 滚动箭头。

### 🎵 语音系统

- **永久移除慧慧**：`getVoiceList()` 硬编码过滤 `huihui`，不受 localStorage 影响
- **晓晓设为默认**：`getSelectedVoice()` 优先搜索 `xiaoxiao`
- **播报打断**：`stopSpeak()` 无条件 `cancel()`，切换步骤时立刻停旧播新
- **手机端语音加载**：空列表时自动发无声 `speak()` 唤醒引擎，`voiceschanged` 自动刷新面板

### ⏱ 计时器系统

- **餐铃音效**：双频（1800Hz+2400Hz）正弦波模拟金属铃"叮"声，0.8s 指数衰减
- **自动模式**：切换步骤后 300ms 自动开始倒计时（默认开启）
- **手动/自动切换**：管理后台可切换，`localStorage` 持久化
- **时间到提示**：铃铛 + 语音 + 计时器区域闪烁（移除阻断式 `alert()`）
- **悬浮倒计时窗**：离开做菜页后底部显示橙色药丸浮窗，三种状态（运行中/已暂停/时间到），点击跳回做菜页
- **计时器持续运行**：离开做菜页/锁屏不暂停，后台继续倒数

### 📱 移动端适配

- **浏览器退回键**：`history.pushState` + `popstate` 监听，退回回到上一页而非退出 App
- **备菜方式切换**：修复缺失的 `togglePrepMode()` 函数

### 📡 跨设备同步

- **导出备份**：11 项 localStorage 数据打包为 JSON 文件下载（API Key 脱敏确认）
- **导入恢复**：文件选择 → 验证版本号 → 写入 localStorage → 刷新页面
- **二维码分享**：LZString 压缩 → QRCode.js 生成 → 另一设备扫码导入
- **扫码导入**：html5-qrcode 摄像头扫描 → 解析 → 恢复 → 自动关窗（失败可关闭）
- **版本校验**：未来版本数据拒绝导入
- **多标签同步**：`storage` 事件监听，标签页间自动同步菜谱/评分/设置
- **localStorage 配额保护**：`saveRecipes()` 捕获 `QuotaExceededError` 弹窗提示

### 🛡️ 边界情况处理

- `localStorage` 写满时弹窗提示而非静默失败
- 导入数据时版本号校验（v > 1 拒绝）
- 所有 `tags` 访问用 `(r.tags || [])` 容错
- 页面隐藏时计时器继续运行（用户反馈后才改了回来）

### 🔧 PWA 支持

新增文件：
- `manifest.json`：App 名、图标、全屏模式、主题色 `#f97316`
- `sw.js`：安装时预缓存核心文件，网络优先 + 3s 超时回退缓存，CDN 资源缓存优先

### 💬 产品策略讨论

- **差异化**：内容壁垒（人工验证菜谱）> 用户数据壁垒 > 品牌信任。别防抄袭，让抄袭成本 > 原创成本
- **出海**：海外中餐教学是蓝海，付费意愿高。需英文本地化 + 食材替代方案。国内测内容，海外挣钱
- **平台测试**：建议先上 GitHub Pages（零成本快速验证），有初步反馈再考虑微信小程序或海外版本

### 📂 项目文件结构

```
cooking-app/
  ├─ index.html          — 主 UI（2740 行）
  ├─ recipes.js          — 51 道菜谱数据（1621 行）
  ├─ manifest.json       — PWA 清单
  ├─ sw.js               — Service Worker（离线缓存）
  └─ 开发日志.md          — 本文件
```

启动方式：`python3 -m http.server 8080`，手机连同一 WiFi 访问 `http://电脑IP:8080`

---

## 2026年7月19日（周六）

### 🚀 发布前评估

对掌厨项目进行了全面评估，确定以下发布前待办：

| 优先级 | 事项 | 进展 |
|--------|------|------|
| 🔴 最高 | 部署上线（GitHub Pages） | 计划 7月20日执行 |
| 🔴 最高 | PWA 图标 | ✅ 已完成（见下） |
| 🟡 中 | API Key 引导优化 | 待做 |
| 🟡 中 | 浏览器兼容测试（iOS Safari / 微信 / Android Chrome） | 待做 |
| 🟢 后续 | 新手引导、隐私说明、CDN 去依赖化 | 待做 |

### 🎨 App 图标设计

- **旧图标**：`manifest.json` 中使用的 emoji SVG（🍳），不够专业
- **设计过程**：
  1. 评估多个 AI 图标工具：Midjourney（最佳质感）、Ideogram（文字渲染）、Flux（画质）、Adobe Firefly（商用安全）、DALL·E 3（上手快）→ 选择 **Midjourney**
  2. 原始概念：「手掌形状的锅 + 锅顶厨师帽」→ 一语双关但过于复杂，缩小后辨识度差，放弃
  3. 最终方案：「橙色圆底 + 白色厨师帽剪影 + 锅铲」→ 极简、2色、缩小到 64px 仍清晰
- **MJ Prompt**：
  ```
  A super minimal app icon, solid warm orange rounded square background,
  a pure white chef hat silhouette centered, a simple white spatula shape
  inside the hat, only 2 colors total, bold thick shapes, no details no
  texture no shadows no gradients, flat vector icon, iOS app icon style
  --style raw --s 50 --ar 1:1
  ```
- **选定文件**：`icon-384.png`（384×384，橙底白色厨师帽+锅铲剪影）
- **技术改动**：
  - `manifest.json`：`icons` 从 emoji SVG 改为 `icon-384.png`
  - `index.html`：新增 `<link rel="apple-touch-icon" href="icon-384.png">`
- **后续建议**：可放大到 512×512 以获得最佳 PWA 兼容性

### 📋 部署计划

- 目标平台：**GitHub Pages**（免费、HTTPS、零运维）
- 用户访问方式：`https://{username}.github.io/掌厨`
- 手机端：浏览器打开 → "添加到主屏幕" → 像原生 App
- 执行日期：2026年7月20日

---

## 2026年7月22日（周二）— 菜谱扩充 51→66 + 小程序版生成

### 🍳 菜谱扩充（+15道）

从51道扩充到66道，新增：
- **湘菜3道**：剁椒鱼头、辣椒炒肉、外婆菜炒蛋
- **凉菜3道**：皮蛋豆腐、凉拌木耳、凉拌鸡丝
- **经典硬菜4道**：红烧狮子头、东坡肉、糖醋排骨（苏式）、粉蒸肉
- **素菜1道**：蒜蓉蒸茄子
- **羊肉1道**：葱爆羊肉
- **豆腐1道**：家常豆腐
- **鸡肉1道**：口水鸡
- **快手主食1道**：炒方便面

### 📱 微信小程序版 — 完整骨架生成

- **AppID**：wx8555bf71750f7e29
- **47个文件**：6页面 / 3组件 / 5工具模块
- 语音：WechatSI 插件（wx069ba97219f66d99）
- AI：wx.request → DeepSeek API
- 存储：wx.setStorageSync，250KB菜谱懒加载
- **模拟器启动失败** → 定位为 recipes.js 250KB 一次性加载导致 → 改为懒加载修复
- **用户备案未完成** → 无法添加域名白名单和插件 → 建议用"不校验域名"模式调试
- 路径：`cooking-miniapp/`

---

## 2026年7月25日（周五）— 赞赏变现功能

### ☕ 赞赏功能

- Commit: `b45b81e`
- 左下角 ☕ 浮动按钮，点击弹出赞赏弹窗
- 微信/支付宝二维码切换（`wechat-qr.png` / `alipay-qr.png`）
- 弹窗有支付方式切换 Tab，分别高亮微信绿和支付宝蓝
- 图片加载失败时自动显示占位提示

### 📋 正规化路径讨论

1. **小程序路径**：注册个体工商户 → 微信认证（¥300/年）→ 添加餐饮服务类目 → 提交审核上架
2. **原生App路径**：个体户注册 → Apple Developer（¥688/年）→ 软著登记（¥300-1000，30-40天）→ Capacitor打包
3. **建议分三步**：网页版先迭代（已上线）→ 小程序上线（1个月内）→ 原生App（用户量起来后）

---

## 2026年7月26日（周六）— 菜谱大扩充 66→159（+93道）

### 🍳 菜谱新增概况

| 分类 | 新增 | 标签总量 |
|------|------|----------|
| 🦐 海鲜 | 20道 | 41 |
| 💪 减脂/健身 | 25道 | 65 |
| 🏮 鲁菜 | 7道 | 12 |
| 🏯 苏菜（新菜系） | 6道 | 6 |
| 🏞️ 浙菜（新菜系） | 6道 | 6 |
| 🏝️ 闽菜（新菜系） | 5道 | 5 |
| 🌶️ 湘菜（新菜系） | 6道 | 6 |
| 🏔️ 徽菜（新菜系） | 3道 | 2 |
| 🍲 汤羹 | +5 | 18 |
| 🥒 凉菜 | +5 | 14 |
| 🥞 早餐 | +4 | 17 |
| 🍚 主食 | +2 | 14 |

### 🦐 海鲜类亮点（20道）

清蒸鲈鱼/石斑鱼、蒜蓉粉丝蒸扇贝/虾、油焖大虾、葱姜炒蟹、红烧带鱼、椒盐皮皮虾、白灼虾、香煎三文鱼、蛤蜊蒸蛋、剁椒鱼头、酸菜鱼、水煮鱼片、葱油蛏子、海蛎煎蛋、盐焗虾、爆炒鱿鱼、紫菜蛋花汤、糖醋鲤鱼、蒜蓉西兰花炒虾仁

### 💪 减脂/健身类亮点（25道）

水煮鸡胸肉（煮1分钟+焖15分钟+冰水浸泡=嫩滑不柴）、蒜蓉蒸鸡胸、鸡胸肉炒西兰花、彩椒鸡丁、番茄鸡胸丸子汤、凉拌鸡丝、凉拌豆腐、木耳拌豆腐、芹菜炒牛肉、西兰花炒牛肉、黑椒牛柳（减油版）、虾仁滑蛋、芦笋炒虾仁、秋葵炒蛋、虾仁豆腐蒸蛋羹、香菇蒸鸡胸、番茄虾滑汤、菌菇鸡肉汤、菠菜蛋花汤、香煎牛排、青椒炒蛋、蒜蓉生菜、牛肉蔬菜卷饼

### 🏮 六大地域菜系新增（32道）

- **鲁菜7道**：葱烧海参、蒜爆羊肉、糖醋里脊（鲁式）、葱烧蹄筋、德州扒鸡、酸菜汆羊肉、炸酱面
- **苏菜6道**：红烧狮子头、糖醋排骨（苏式/两遍糖醋法）、大煮干丝、盐水鸭、粉蒸肉、松鼠桂鱼
- **浙菜6道**：西湖醋鱼（草鱼做出蟹味）、东坡肉、龙井虾仁、荷叶粉蒸肉、叫花鸡（烤箱版）、响油黄瓜
- **闽菜5道**：佛跳墙（家常版）、荔枝肉、沙茶牛肉、芋泥、海蛎煎蛋
- **湘菜6道**：辣椒炒肉、外婆菜炒蛋、腊肉炒蒜薹、毛氏红烧肉、口味虾（麻辣小龙虾）、剁椒鱼头
- **徽菜3道**：臭鳜鱼（家常版）、红烧划水（鱼尾）、毛豆腐预留

### 📊 数据统计

- `recipes.js`：4,314行，159道完整菜谱
- 标签：18种（新增苏菜/浙菜/闽菜/湘菜/徽菜）
- 难度分布：入门67 / 家常73 / 进阶19
- 减脂标签从14→65（+364%）
- 海鲜标签从6→41（+583%）
- 备份：`recipes.js.bak`（66道旧版）
- 小程序版 recipes 待同步更新

---

## 2026年7月27日（周日）— 网页端性能优化 + recipes.js 括号 Bug 修复

### ⚡ 性能优化

**问题**：页面加载时白屏 3-5 秒，500KB `recipes.js` 在 `<head>` 中阻塞渲染。

**优化项**：

| 改动 | 文件 | 效果 |
|------|------|------|
| `recipes.js` 从 `<head>` 移到 `</body>` 前 | `index.html` | 页面 UI 立即渲染，不再白屏等 500KB |
| 3 个 CDN 脚本加 `defer`，移出 `<head>` | `index.html` | 不阻塞 HTML 解析，QR 功能按需加载 |
| 百度统计从 `<head>` 移到 `</body>` 前 | `index.html` | 不阻塞首屏渲染 |
| DNS 预解析（jsdelivr/unpkg/百度） | `index.html` | 减少外部域名解析延迟 |
| 骨架屏 shimmer 动画（3 张占位卡片） | `index.html` | 菜谱加载中有视觉反馈 |
| `loadRecipes()` 防御性检查 | `index.html` | `DEFAULT_RECIPES` 未定义时返回空对象，不崩溃 |
| `isCustomRecipe()` 防御性检查 | `index.html` | `DEFAULT_RECIPES` 未定义时不抛 TypeError |

**SW 优化** (`sw.js`)：
- 修复 fetch 超时后缓存为空时 `resolve` 永不调用的 dangling promise bug
- 升级 `CACHE_VERSION` 到 `v5`
- CDN 资源改用 stale-while-revalidate（立即返回缓存，后台静默更新）

### 🐛 recipes.js 括号 Bug 修复

**问题**：7月26日菜谱扩充时，`chaofangbianmian`（炒方便面）菜谱末尾少了一个 `},`：
```diff
     ],
+  },
   // ===== 海鲜类 (20道) =====
```
导致 `recipes.js` 有 JS 语法错误，`const DEFAULT_RECIPES` 无法解析，浏览器报 `Uncaught SyntaxError: Unexpected token ';'`。

**为什么之前没发现**：`loadRecipes()` 优先从 localStorage 读取。已访问过的用户 localStorage 存有完整菜谱，永不触发 `DEFAULT_RECIPES` 回退路径。只有全新 origin（如 `localhost:8080`）首次访问才暴露。

**修复**：在第 2178 行 `    ],` 之后补上 `  },`，语法检查通过（`node --check` PASS）。

### 📝 技术备忘

- 用 Node.js `--check` 验证 JS 语法，比浏览器 console 更快发现括号不平衡
- 二分查找 + `balance` 追踪定位缺失括号：3162 个 `{` vs 3161 个 `}` → 差 1
- `file://` 协议下浏览器可能限制加载本地脚本，本地测试建议用 `python -m http.server`
- 函数声明写在 `if` 块内（`function renderNext() {...}`）在某些环境有兼容性问题 → 改用 `var renderNext = function() {...}`
